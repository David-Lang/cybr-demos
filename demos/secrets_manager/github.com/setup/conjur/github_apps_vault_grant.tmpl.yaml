# metadata
# mode: append-policy
# Applied at data root: vault/... -> data/vault/... ; github-apps/... -> data/github-apps/...
---
- !grant
  roles:
    - !group vault/{{ .SAFE_NAME }}/delegation/consumers
  members:
    - !host github-apps/{{ .GITHUB_WORKFLOW }}
