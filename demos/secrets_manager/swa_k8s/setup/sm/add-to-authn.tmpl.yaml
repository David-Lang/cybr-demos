# mode: append-policy
---
- !policy
  id: {{ .SM_SERVICE_NAME }}
  body:
    - !grant
      roles:
        - !group apps
      members:
        - !host "/data/swa-workloads/{{ .JWT_CLAIM_IDENTITY_VALUE }}"
