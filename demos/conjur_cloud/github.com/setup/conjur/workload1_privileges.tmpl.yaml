# metadata
# mode: append-policy
---
# This policy is applied to the /data branch.
# Grants safe access to a workload.
- !grant
  roles:
    - !group vault/{{ .SAFE_NAME }}/delegation/consumers
  members:
    - !host workloads/{{ .JWT_CLAIM_IDENTITY }}
